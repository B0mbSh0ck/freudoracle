# 🚀 Быстрый старт Oracle Bot

## 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его (Windows)
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

## 2. Настройка окружения

```bash
# Скопируйте шаблон конфигурации
copy .env.example .env
```

Откройте `.env` и заполните:

### 2.1 Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username)
4. Скопируйте полученный токен в `.env`:
   ```
   TELEGRAM_BOT_TOKEN=your_token_here
   ```

### 2.2 Получение OpenAI API Key

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в API Keys
3. Создайте новый ключ
4. Скопируйте в `.env`:
   ```
   OPENAI_API_KEY=sk-...
   AI_PROVIDER=openai
   AI_MODEL=gpt-4-turbo-preview
   ```

**Альтернатива - Anthropic Claude:**
```
ANTHROPIC_API_KEY=sk-ant-...
AI_PROVIDER=anthropic
AI_MODEL=claude-3-opus-20240229
```

## 3. Инициализация проекта

```bash
# Инициализируйте базу данных
python init_project.py
```

## 4. Запуск бота

```bash
# Запустите бота
python main.py
```

Вы должны увидеть:
```
Starting Oracle Bot...
```

## 5. Тестирование

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Задайте вопрос!

## 📋 Чеклист перед запуском

- [ ] Python 3.11+ установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены
- [ ] .env файл создан и заполнен
- [ ] Telegram Bot Token получен
- [ ] OpenAI API Key получен (и есть баланс)
- [ ] База данных инициализирована
- [ ] Бот запущен

## ⚠️ Важные замечания

1. **OpenAI API стоит денег** - убедитесь что у вас есть баланс на аккаунте
2. **Начните с GPT-3.5** если хотите сэкономить:
   ```
   AI_MODEL=gpt-3.5-turbo
   ```
3. **Логи** сохраняются в директории `logs/`
4. **База данных** SQLite создается автоматически: `oracle.db`

## 🐛 Troubleshooting

### Ошибка: "No module named 'config'"
Убедитесь что вы запускаете из корневой директории проекта.

### Ошибка: "Invalid API key"
Проверьте правильность API ключа в `.env`

### Ошибка: "pyswisseph not found"
```bash
pip install pyswisseph==2.10.3.2
```

### Бот не отвечает
1. Проверьте что бот запущен (не закрыли терминал)
2. Проверьте логи в `logs/bot.log`
3. Проверьте что у вас есть интернет

## 🚀 Что дальше?

1. **Протестируйте базовый функционал**
2. **Настройте промпты** в `oracle/interpreter.py`
3. **Добавьте полную базу И-Цзин** (64 гексаграммы)
4. **Реализуйте монетизацию** (Telegram Payments)
5. **Добавьте голосовые сообщения** (Whisper API)
6. **Развернуть на сервере** (VPS, Cloud)

## 📞 Поддержка

Если что-то не работает, проверьте:
1. Логи: `logs/bot.log`
2. Консоль (терминал) где запущен бот
3. Правильность .env конфигурации
