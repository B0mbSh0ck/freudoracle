# 📁 Структура проекта Oracle Bot

```
orc/
│
├── 📄 README.md                    # Общее описание проекта
├── 📄 QUICKSTART.md                # Быстрый старт (начни здесь!)
├── 📄 ROADMAP.md                   # План развития и монетизации
├── 📄 EXAMPLES.md                  # Примеры использования
├── 📄 PROJECT_STRUCTURE.md         # Этот файл
│
├── 📄 requirements.txt             # Python зависимости
├── 📄 .env.example                 # Шаблон конфигурации
├── 📄 .gitignore                   # Git ignore файл
│
├── 📄 main.py                      # 🚀 ГЛАВНЫЙ ФАЙЛ - запуск бота
├── 📄 init_project.py              # Скрипт инициализации
├── 📄 test_oracle.py               # Тестирование модулей
│
├── 📂 config/                      # ⚙️ КОНФИГУРАЦИЯ
│   ├── __init__.py
│   └── settings.py                 # Настройки из .env
│
├── 📂 bot/                         # 🤖 TELEGRAM BOT (будущее расширение)
│   ├── handlers/                   # Обработчики команд
│   ├── keyboards/                  # Клавиатуры
│   └── middleware/                 # Middleware
│
├── 📂 oracle/                      # 🔮 ЯДРО ОРАКУЛА
│   ├── __init__.py
│   │
│   ├── 📂 iching/                  # 📖 И-ЦЗИН (Книга Перемен)
│   │   ├── __init__.py
│   │   └── iching.py               # Логика бросания монет и гексаграмм
│   │
│   ├── 📂 tarot/                   # 🎴 ТАРО
│   │   ├── __init__.py
│   │   └── tarot.py                # 22 Старших Аркана + расклады
│   │
│   ├── 📂 horary/                  # ⭐ ХОРАРНАЯ АСТРОЛОГИЯ
│   │   ├── __init__.py
│   │   └── horary.py               # Расчет хорарной карты
│   │
│   ├── 📂 ritual/                  # 🧘 ГЕНЕРАТОР РИТУАЛОВ
│   │   ├── __init__.py
│   │   └── ritual_generator.py     # По методу Виногродского
│   │
│   └── 📄 interpreter.py           # 🤖 AI ИНТЕРПРЕТАТОР (объединяет все)
│
├── 📂 database/                    # 💾 БАЗА ДАННЫХ
│   ├── __init__.py
│   ├── database.py                 # Подключение к БД
│   └── models.py                   # SQLAlchemy модели
│
├── 📂 data/                        # 📚 СТАТИЧЕСКИЕ ДАННЫЕ
│   └── iching_hexagrams.json       # База 64 гексаграмм И-Цзин
│
└── 📂 logs/                        # 📝 ЛОГИ (создается автоматически)
    └── bot.log
```

## 🎯 Ключевые файлы

### Для запуска:
1. **main.py** - запуск Telegram бота
2. **init_project.py** - первичная инициализация
3. **test_oracle.py** - проверка работоспособности

### Для настройки:
1. **.env** (создать из .env.example) - все секреты и ключи
2. **config/settings.py** - чтение настроек
3. **data/iching_hexagrams.json** - база гексаграмм

### Для кастомизации:
1. **oracle/interpreter.py** - стиль ответов Оракула
2. **oracle/ritual/ritual_generator.py** - стиль ритуалов
3. **oracle/tarot/tarot.py** - карты Таро

## 📊 Зависимости между модулями

```
main.py (Telegram Bot)
    ↓
oracle/interpreter.py (Координатор)
    ↓
    ├─→ oracle/iching/iching.py (И-Цзин)
    ├─→ oracle/tarot/tarot.py (Таро)
    ├─→ oracle/horary/horary.py (Хорары)
    └─→ AI API (OpenAI/Anthropic)

oracle/ritual/ritual_generator.py
    ↑
    └── main.py (по запросу /ritual)
```

## 🔧 Модули проекта

### 1. Config (Конфигурация)
**Файлы:** `config/settings.py`

**Что делает:**
- Загружает настройки из `.env`
- Управляет API ключами
- Настройки монетизации

**Используется:**
- Всеми модулями для доступа к настройкам

---

### 2. Oracle / I-Ching (И-Цзин)
**Файлы:** `oracle/iching/iching.py`, `data/iching_hexagrams.json`

**Что делает:**
- Виртуальное бросание 3 монет × 6 раз
- Определение гексаграммы (первичной и результирующей)
- Интерпретация изменяющихся линий

**Основные классы:**
- `Hexagram` - dataclass гексаграммы
- `IChing` - логика работы с И-Цзин
- `iching` - singleton instance

**Ключевые методы:**
- `cast_coins()` - бросить монеты
- `format_hexagram()` - форматировать для вывода

---

### 3. Oracle / Tarot (Таро)
**Файлы:** `oracle/tarot/tarot.py`

**Что делает:**
- 22 Старших Аркана с интерпретациями
- Прямые и перевернутые значения
- Расклады (карта дня, 3 карты)

**Основные классы:**
- `TarotCard` - карта Таро
- `TarotDeck` - колода
- `TarotReading` - чтения
- `tarot` - singleton instance

**Ключевые методы:**
- `card_of_the_day()` - карта дня
- `three_card_spread()` - расклад на 3 карты

---

### 4. Oracle / Horary (Хорарная астрология)
**Файлы:** `oracle/horary/horary.py`

**Что делает:**
- Расчет планет на момент вопроса
- Определение домов (система Плацидуса)
- Базовая интерпретация

**Основные классы:**
- `Planet` - планета в карте
- `HoraryChart` - хорарная карта
- `HoraryAstrology` - движок расчетов
- `horary` - singleton instance

**Зависимости:**
- Swiss Ephemeris (pyswisseph)

---

### 5. Oracle / Interpreter (AI Интерпретатор)
**Файлы:** `oracle/interpreter.py`

**Что делает:**
- Объединяет все 3 метода гадания
- Отправляет в AI (OpenAI/Anthropic)
- Генерирует мудрый ответ Оракула

**Основные классы:**
- `OracleInterpreter`
- `oracle_interpreter` - singleton

**Ключевые методы:**
- `process_question()` - основной метод
- `generate_followup_response()` - уточняющие вопросы

---

### 6. Oracle / Ritual (Генератор ритуалов)
**Файлы:** `oracle/ritual/ritual_generator.py`

**Что делает:**
- Создает психологические ритуалы
- На основе метода Виногродского
- Использует символизм гадания

**Основные классы:**
- `RitualGenerator`
- `ritual_generator` - singleton

**Ключевые методы:**
- `generate_ritual()` - создать ритуал

---

### 7. Database (База данных)
**Файлы:** `database/models.py`, `database/database.py`

**Что делает:**
- Хранит пользователей
- История вопросов
- Ритуалы
- Платежи (для монетизации)

**Модели:**
- `User` - пользователь
- `QuestionSession` - сессия вопроса
- `FollowUpQuestion` - уточняющие вопросы
- `Ritual` - сохраненные ритуалы
- `Payment` - платежи

---

### 8. Main (Telegram Bot)
**Файлы:** `main.py`

**Что делает:**
- Интерфейс Telegram бота
- Обработка команд и сообщений
- Orchestration всех модулей

**Основные классы:**
- `OracleBot` - главный класс бота

**Команды:**
- `/start` - приветствие
- `/ask` - задать вопрос
- `/ritual` - получить ритуал
- `/stats` - статистика
- `/help` - помощь

---

## 🔄 Типичный flow запроса

```
1. Пользователь → Telegram → /ask "Мой вопрос"
                    ↓
2. main.py → OracleBot.handle_message()
                    ↓
3. oracle_interpreter.process_question()
                    ↓
4. Параллельно:
   ├─→ iching.cast_coins()          → Гексаграмма
   ├─→ tarot.card_of_the_day()      → Карта
   └─→ horary.calculate_chart()     → Хорарная карта
                    ↓
5. Данные → AI API (OpenAI/Anthropic)
                    ↓
6. AI → Интерпретация (300-500 слов)
                    ↓
7. Результат → Пользователю в Telegram
                    ↓
8. Опционально: /ritual → ritual_generator
```

## 📦 Как добавить новый модуль гадания

Допустим, вы хотите добавить **Руны**:

1. Создайте `oracle/runes/runes.py`
2. Реализуйте логику:
   ```python
   class RuneCast:
       def cast_rune(self):
           # логика
           pass
   
   runes = RuneCast()
   ```

3. Добавьте в `oracle/interpreter.py`:
   ```python
   from oracle.runes.runes import runes
   
   # В process_question():
   rune = runes.cast_rune()
   ```

4. Обновите промпт для AI чтобы учитывать Руны

## 🚀 Развертывание

### Локально (разработка)
```bash
python main.py
```

### VPS (продакшн)
```bash
# Используйте systemd service или supervisor
# Пример systemd unit:

[Unit]
Description=Oracle Bot
After=network.target

[Service]
Type=simple
User=oracle
WorkingDirectory=/home/oracle/orc
ExecStart=/home/oracle/orc/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

### Docker (опционально)
```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "main.py"]
```

## 📈 Мониторинг

Логи сохраняются в:
- `logs/bot.log` - все логи бота
- Консоль (stdout) - в реальном времени

Используйте `loguru` для добавления своих логов:
```python
from loguru import logger

logger.info("Что-то произошло")
logger.error("Ошибка!")
```

## 🔐 Безопасность

**Важно:**
- Не коммитьте `.env` файл в git
- Храните API ключи в безопасности
- Регулярно обновляйте зависимости
- Ограничьте доступ к БД

## 📞 Поддержка

При возникновении проблем:
1. Проверьте `logs/bot.log`
2. Запустите `python test_oracle.py`
3. Проверьте `.env` конфигурацию

---

**Версия:** 1.0.0-MVP  
**Дата:** 2026-01-15  
**Автор:** Oracle Bot Development Team
