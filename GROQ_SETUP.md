# 🚀 Настройка Groq (Бесплатно) с откатом на OpenAI

Эта конфигурация позволяет использовать бесплатный и быстрый Groq API, но автоматически переключаться на OpenAI, если ключа Groq нет или он не работает.

## 1. Получение ключа Groq (2 минуты)

1. Перейдите на [console.groq.com](https://console.groq.com/)
2. Войдите через Google или Email.
3. В меню слева выберите **API Keys**.
4. Нажмите **Create API Key**.
5. Скопируйте ключ (начинается на `gsk_...`).

## 2. Настройка .env файла

Откройте файл `.env` и добавьте/измените следующие строки:

```ini
# --- AI НАСТРОЙКИ ---

# Ваш основной платный ключ (оставьте его для страховки)
OPENAI_API_KEY=sk-proj-...

# Ваш новый бесплатный ключ Groq
GROQ_API_KEY=gsk_...

# --- ПЕРЕКЛЮЧАТЕЛЬ ---

# Вариант А: Включить Groq (Бесплатно + Быстро)
AI_PROVIDER=groq
AI_MODEL=llama-3.3-70b-versatile

# Вариант Б: Вернуться на OpenAI (Платно + GPT-4)
# AI_PROVIDER=openai
# AI_MODEL=gpt-4-turbo-preview
```

## 3. Как это работает (Логика отката)

В коде (`oracle/interpreter.py`) реализована "умная" логика:

1. Бот проверяет `AI_PROVIDER`.
2. Если стоит `groq`:
   - Проверяет, есть ли `GROQ_API_KEY`.
   - ✅ **Есть:** Использует Groq (Llama-3).
   - ❌ **Нет:** Автоматически переключается на OpenAI (GPT-4) используя `OPENAI_API_KEY`.
3. Если стоит `openai`:
   - Всегда использует OpenAI.

## 4. Проверка

После настройки перезапустите бота:

```bash
python main.py
```

В логах вы увидите:
- `🚀 Groq API активирован` — если работает Groq.
- `⚠️ Groq ключ не найден, используем OpenAI` — если сработал откат.

---

**Рекомендуемая модель для Groq:** `llama3-70b-8192` (самая умная) или `mixtral-8x7b-32768`.
