# Аудит Навигации Бота - Результаты

## Обнаруженные Проблемы

### 1. ❌ КРИТИЧНО: Дублирующаяся кнопка "Подробнее" после ответа
**Локация:** `main.py:390` и `main.py:393`
```python
[InlineKeyboardButton("🔍 Подробнее", callback_data="ask_details")],  # Строка 390
[
    InlineKeyboardButton("🧠 Психолог", url="https://t.me/hypnotic_fire"),
    InlineKeyboardButton("🔍 Детали расклада", callback_data="details")  # Строка 393
]
```
**Проблема:** Две похожие кнопки с разными callback (`ask_details` и `details`), но `ask_details` НЕ обрабатывается в `button_handler`!

### 2. ❌ Логическая ошибка в обработчике "details"
**Локация:** `main.py:1029-1052`
```python
elif query.data == "details":
    if 'last_oracle_response' in context.user_data:
        # ... показываем детали ...
        await query.message.reply_text(fix_markdown(details), parse_mode='Markdown')
    else:
        await query.message.reply_text("⚠️ Сначала задай вопрос!")
    
    # ЭТО ВЫПОЛНЯЕТСЯ ВСЕГДА! Даже если деталей нет
    await query.message.reply_text("🗣 Что именно ты хочешь уточнить? Напиши свой вопрос.")
    context.user_data['awaiting_followup'] = True
    return
```
**Проблема:** Отступы неправильные - запрос на уточнение отправляется ВСЕГДА, даже когда показали "Сначала задай вопрос!"

### 3. ⚠️ Отсутствует обработчик `ask_details`
**Используется в:** `main.py:390`
**Не найдено в:** `button_handler`
**Результат:** Кнопка "🔍 Подробнее" не работает после основного ответа

### 4. ⚠️ Несогласованность кнопок "Назад"
- В `ask_command`: "🔙 Назад" → `menu`
- В `dream_command`: "🔙 Назад" → `menu`  
- В `help_command`: "🔙 Назад" → `menu`
- В главном меню: "🔙 Назад" → `start_msg`

**Вопрос:** Должна ли кнопка "Назад" в главном меню вести на `/start` или это корректно?

## Рекомендации по Исправлению

### Приоритет 1: Исправить обработчик "details"
Нужно правильно расставить отступы, чтобы запрос уточнения был только после показа деталей.

### Приоритет 2: Добавить обработчик "ask_details"
Либо добавить обработку `ask_details`, либо заменить на `details` в клавиатуре после ответа.

### Приоритет 3: Убрать дублирование кнопок
Оставить одну кнопку "Подробнее" с понятным назначением.
